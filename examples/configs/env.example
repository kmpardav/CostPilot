# Copy to .env.local and replace placeholder values before running the CLI.
# The CLI also respects AZURECOST_* defaults (region/currency) if you want to override them here.
OPENAI_API_KEY=replace-with-your-key
AZURECOST_DEFAULT_REGION=westeurope
AZURECOST_DEFAULT_CURRENCY=EUR
